version: "3"

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: endgame-dev
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
      # command: ["postgres", "-c", "log_statement=all"]
    shm_size: 1gb
